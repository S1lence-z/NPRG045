type AmpPhaseDataPoint = {
    x: number | string;
    y: number | string;
    timestamp: string;
    distance: number;
    amplitude: number;
    phase: number;
};

export default AmpPhaseDataPoint;
